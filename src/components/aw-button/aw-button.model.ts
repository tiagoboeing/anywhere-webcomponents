export enum AwButtonStatus {
  primary = 'primary',
  secondary = 'secondary',
  success = 'success',
  danger = 'danger',
  warn = 'warn',
}

export enum AwButtonMode {
  default = 'default',
  outline = 'outline',
}
