export enum AwButtonStatus {
  primary = 'primary',
  secondary = 'secondary',
  success = 'success',
  danger = 'danger',
  warn = 'warn',
  info = 'info',
}

export enum AwButtonSize {
  tiny = 'tiny',
  small = 'small',
  medium = 'medium',
  large = 'large',
  giant = 'giant',
}

export enum AwButtonMode {
  rounded = 'rounded',
  square = 'square',
  radius = 'radius',
}

export enum AwButtonColor {
  solid = 'solid',
  gradient = 'gradient',
  outline = 'outline',
  basic = 'basic',
}

export enum AwButtonIconMode {
  left = 'left',
  rigth = 'right',
}
